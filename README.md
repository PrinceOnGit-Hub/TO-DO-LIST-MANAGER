# TO-DO-LIST-MANAGER
I created a to-do list application using a singly linked list to manage tasks and an array to store task descriptions. All tasks are saved to a .txt file, ensuring persistence. When the program is closed, pending tasks are stored, and when the user reopens the program, the tasks are loaded from the file.
